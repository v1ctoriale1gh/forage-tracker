<h1>Here's all your item's by date harvested!</h1>
<% @user.dated_harvests.each do |harvests| %>
<ul>
    <% harvests.each do |harvest| %>
        <li><%= harvest.expedition.date_and_time %> <%= harvest.item.name %> - <%= harvest.amount %> <%= link_to "View Item", item_path(harvest.item_id) %> </li>
    <% end %>
    </ul>
<% end %>
